import styles from "./Footer.module.scss";

const Footer = () => {
    return (
        <footer className={styles.footer}>
            <span>HackIllinois</span>
            <span>Privacy Policy</span>
        </footer>
    );
};

export default Footer;
